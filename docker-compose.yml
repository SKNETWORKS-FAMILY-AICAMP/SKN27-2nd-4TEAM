version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: churn-db
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: churn_db
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data